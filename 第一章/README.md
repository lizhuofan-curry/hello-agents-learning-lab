# 第一章｜让 Agent 跑出第一个闭环

这一章用一个旅行助手回答最朴素的问题：模型说“下一步查天气”，程序如何真的调用天气工具，再把结果交还给模型？我把重点放在 **Thought → Action → Observation → 下一步** 的循环，而不是把一次模型回复误当作完整 Agent。

> 想象你问：“今天适合去哪儿玩？”助手不能只凭印象编天气；它得先查、再根据结果找景点，最后把理由说清楚。这就是本章的故事线。

## 你会看到什么

1. **模型提出动作**：输出约定格式的 `Thought` 和 `Action`，例如请求 `get_weather`。
2. **程序解析并执行**：从文本中识别工具名和参数，在 `available_tools` 中找到真正的 Python 函数。
3. **环境返回观察**：天气或景点结果成为 `Observation`，进入下一轮上下文。
4. **有条件地停止**：模型用 `Finish[...]` 给出最终答案；脚本最多尝试 5 轮，避免无限循环。

关键区分是：**模型写出“我要调用工具”不等于工具已经运行**。真正的外部动作发生在程序解析并调用函数时。

## 代码入口

- [动手体验：5分钟实现第一个智能体.py](./动手体验：5分钟实现第一个智能体.py)：天气查询、Tavily 景点搜索、动作解析和最终回答都写在同一个脚本里，方便逐行追踪。
- [第一章课后习题](../notes/第一章/第一章的问题.md)：智能体定义、闭环、记忆、幻觉、循环上限与评价。

建议按 `AGENT_SYSTEM_PROMPT → get_weather/get_attraction → available_tools → 主循环` 的顺序阅读。这样一行 `Observation` 从哪里来、又被送回哪里，会比从头到尾机械读一遍清楚得多。

## 运行

在仓库根目录使用 PowerShell：

```powershell
uv sync --group chapter1
Copy-Item .env.example .env
# 编辑 .env：填写 LLM_API_KEY、LLM_MODEL_ID、LLM_BASE_URL、TAVILY_API_KEY
uv run --group chapter1 python ".\第一章\动手体验：5分钟实现第一个智能体.py"
```

脚本会访问模型、天气和搜索服务；服务可用性、额度与结果都可能变化。阅读时可以重点观察：模型输出的 Action 如何被解析，工具结果怎样成为下一轮的 Observation，以及格式不符合约定时会怎样。这里的文本动作解析是教学实现，不是可信任的通用工具执行沙箱。

## 给自己一个小实验

- 把问题从“推荐景点”改成“下雨时推荐室内活动”，观察模型是否仍先查天气。
- 暂时让天气工具返回一个固定的“雨天”字符串，单独研究后续行动；这是**模拟结果**，不要当成实时天气。
- 思考如果模型把 `Action` 写错一字，当前正则解析会怎样；再到[第四章](../第四章/README.md)看 ReAct 的同类问题。

我的笔记里还进一步问：为什么 Agent 需要终止条件？只有答案看似正确，是否足够证明它的过程可靠？这些问题比“脚本跑通了”更值得带走。

[返回主页](../README.md) · [下一章：ELIZA](../第二章/README.md)
